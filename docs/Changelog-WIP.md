This documents changes pending and not published in a released version:

<!-- TODO (2026-07-27, ccharland): Include in next release changelog entry:
- `auto-tag-publish.yml` — updated `actions/github-script` from v7 (Node.js 20) to v8 (Node.js 22) to resolve GitHub Actions deprecation warning (#84)
- `publish.yml` — improved tag signature verification: uses full `refs/tags/` ref path, force-fetches tags from origin to avoid stale refs, validates annotated tag object type before verify, and improved `awk` parsing for signer fingerprint (#84)
-->